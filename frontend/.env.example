# Local dev: point at the backend directly.
VITE_API_BASE_URL=http://localhost:8000/api

# Vercel production: leave unset OR set to /api (same-origin proxy via vercel.json).
# Do NOT set to https://credenceai-api.onrender.com/api — that causes CORS errors.
# VITE_API_BASE_URL=/api
